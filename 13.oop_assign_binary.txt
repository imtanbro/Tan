#include<iostream>
#include<list>
using namespace std;
void display(list<int> &l3)
{
    list<int>::iterator p;
    for(p=l3.begin();p!=l3.end();p++)
    {
        cout<<*p;
    }
}
int main()
{
	int a,b,p,q,carry=0,sum,ans;
	cout<<"Enter length of first binary number : ";
	cin>>a;
	cout<<"Enter length of second binary number : ";
	cin>>b;
	list<int> l1;
	list<int> l2;
	int max,min;
	if(a>b)
	{ 
		max=a;
		min=b;
	}
	else if(b>a)
	{
		max=b;
		min=a;
	}
	else
	{ 
		max=a;
		min=a;
	}
	list<int> l3;
	cout<<"Enter the bits of first number from MSB to LSB:\n";
	for(int i=0;i<a;i++)
	{
		cout<<"Enter bit :";
		cin>>p;
		if(p==1||p==0)
		{
			
			l1.push_back(p);
		}
		else
		{
			cout<<"* Enter only 0 or 1 *\n";
			i--;
		}
	}
	cout<<"\nFirst binary number entered is : ";
	display(l1);
	cout<<"\nEnter the bits of second number from MSB to LSB:\n";
	for(int i=0;i<b;i++)
	{
		cout<<"Enter bit :";
		cin>>q;
		if(q==1||q==0)
		{	
			l2.push_back(q);
		}
		else
		{
			cout<<"* Enter only 0 or 1 *\n";
			i--;
		}
	}
	cout<<"\nSecond binary number entered is : ";
	display(l2);
	for(int i=0;i<min;i++)
	{
		sum=(l1.back()+l2.back()+carry)%2;
		carry=(l1.back()+l2.back()+carry)/2;
		l2.pop_back();
		l1.pop_back();
		l3.push_back(sum);
	}
	while(l1.size()!=0)
	{
		sum=(l1.back()+carry)%2;
		carry=(carry+l1.back())/2;
		l1.pop_back();
		l3.push_back(sum);
	}
	while(l2.size()!=0)
	{   
		sum=(l2.back()+carry)%2;
		carry=(carry+l2.back())/2;
		l2.pop_back();
		l3.push_back(sum);
	}
	if(carry==1)
	{
		l3.push_back(carry);
	}
	cout<<"\nAddition is : ";
	while(l3.size()!=0)
	{
		ans=l3.back();
		cout<<ans;
		l3.pop_back();
	}
	cout<<endl;
	return 0;
}

Output:

Enter length of first binary number : 3
Enter length of second binary number : 2
Enter the bits of first number from MSB to LSB:
Enter bit :1
Enter bit :1
Enter bit :1

First binary number entered is : 111
Enter the bits of second number from MSB to LSB:
Enter bit :1
Enter bit :1

Second binary number entered is : 11
Addition is : 1010

